# Fase 3 — Blog (MDX) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) ou superpowers:executing-plans. Steps usam checkbox (`- [ ]`).
>
> **⚠️ Commits:** Por preferência do dono (CLAUDE.md), **NÃO commitar por task nem por ciclo TDD.** Subagentes só implementam, testam e reportam. Commit único no fim da fase, com aprovação. **Sem push.**

**Goal:** Blog funcional em MDX no repositório: lista `/blog` com filtro por tag, post individual `/blog/[slug]` com render MDX + `BlogPosting` JSON-LD + reading-time, e preview dos 3 posts recentes na landing. Bilíngue PT/EN.

**Architecture:** Sobre Fases 1-2. Posts em `content/blog/<locale>/<slug>.mdx` com frontmatter YAML. `@next/mdx` (oficial Next 16) renderiza o post via import dinâmico; `gray-matter` lê frontmatter para listar/ordenar/filtrar (sem compilar). Lógica pura (parse, sort, filtro, JSON-LD) isolada em `lib/blog/` e `lib/seo/` — testável. Render é Server Component; só o filtro de tags da lista é client.

**Tech Stack:** Next.js 16, `@next/mdx` + `@mdx-js/loader` + `@mdx-js/react`, `remark-frontmatter`, `remark-mdx-frontmatter`, `gray-matter`, `reading-time`, Vitest.

---

## Convenções herdadas (Fases 1-2)

- `Locale='pt'|'en'`, `locales`, `isLocale` em `@/lib/i18n/locales`; `getDictionary`/`Dictionary` em `@/lib/i18n/dictionaries`.
- `SITE_URL` em `@/lib/seo/site-url`; `buildMetadata`/`buildAlternates` em `@/lib/seo/metadata`; `JsonLd` em `@/components/json-ld`.
- `SectionHeading` em `@/components/sections/section-heading`. Paleta: `stone-*` + `amber-600`. `font-mono` p/ labels.
- Páginas: `params: Promise<{...}>` + `await params`.
- Nav já linka `/${locale}/blog` (Fase 1).

---

## File Structure

| Arquivo | Responsabilidade |
|---------|------------------|
| `mdx-components.tsx` | (raiz) componentes MDX estilizados (Warm Workshop) — obrigatório p/ @next/mdx |
| `next.config.ts` | (modifica) envolve com `createMDX`, `pageExtensions`, remark plugins |
| `content/blog/pt/estruturando-microservicos-node.mdx` | Post seed PT |
| `content/blog/en/structuring-node-microservices.mdx` | Post seed EN |
| `lib/blog/types.ts` | tipo `PostMeta` |
| `lib/blog/parse.ts` | `parsePostMeta(raw, slug)` puro (gray-matter + reading-time) |
| `lib/blog/posts.ts` | `getAllPosts(locale)`, `getPostSlugs(locale)` (FS) |
| `lib/blog/filter.ts` | `filterByTag(posts, tag)`, `allTags(posts)` puros |
| `lib/seo/blog-jsonld.ts` | `buildBlogPostingJsonLd(meta, locale)` puro |
| `components/blog/post-list.tsx` | lista + filtro de tag (client) |
| `components/sections/blog-preview.tsx` | 3 posts recentes (landing) |
| `app/[locale]/blog/page.tsx` | página da lista |
| `app/[locale]/blog/[slug]/page.tsx` | post (render + metadata + JSON-LD) |
| `app/[locale]/page.tsx` | (modifica) injeta BlogPreview |
| `app/sitemap.ts` | (modifica) inclui URLs dos posts |

---

## Task 1: Dependências + config MDX

**Files:**
- Modify: `next.config.ts`
- Create: `mdx-components.tsx`

- [ ] **Step 1: Instalar deps**

Run:
```bash
npm i @next/mdx @mdx-js/loader @mdx-js/react @mdx-js/mdx
npm i -D @types/mdx gray-matter reading-time remark-frontmatter remark-mdx-frontmatter
```
Justificativa: pilha oficial `@next/mdx` (render MDX em RSC no Next 16); `gray-matter`/`reading-time` p/ metadados da lista; `remark-*frontmatter` transforma YAML em `export metadata`. Sem contentlayer (abandonado).

- [ ] **Step 2: Configurar `next.config.ts`**

Preservar `output: 'standalone'`. Envolver com `createMDX`:
```typescript
import type { NextConfig } from 'next'
import createMDX from '@next/mdx'
import remarkFrontmatter from 'remark-frontmatter'
import remarkMdxFrontmatter from 'remark-mdx-frontmatter'

const nextConfig: NextConfig = {
  output: 'standalone',
  pageExtensions: ['ts', 'tsx', 'md', 'mdx'],
}

const withMDX = createMDX({
  options: {
    remarkPlugins: [remarkFrontmatter, [remarkMdxFrontmatter, { name: 'frontmatter' }]],
  },
})

export default withMDX(nextConfig)
```
> Frontmatter YAML vira `export const frontmatter` no módulo MDX (usado no render do post p/ título/data).

- [ ] **Step 3: Criar `mdx-components.tsx` na raiz (Warm Workshop)**

```tsx
import type { MDXComponents } from 'mdx/types'

const components: MDXComponents = {
  h1: ({ children }) => <h1 className="text-2xl font-bold text-stone-50 mt-10 mb-4">{children}</h1>,
  h2: ({ children }) => <h2 className="text-xl font-bold text-stone-100 mt-8 mb-3">{children}</h2>,
  h3: ({ children }) => <h3 className="text-lg font-semibold text-stone-100 mt-6 mb-2">{children}</h3>,
  p: ({ children }) => <p className="text-stone-300 leading-relaxed my-4">{children}</p>,
  a: ({ children, href }) => <a href={href} className="text-amber-500 hover:text-amber-400 underline underline-offset-2">{children}</a>,
  ul: ({ children }) => <ul className="list-disc pl-6 my-4 text-stone-300 space-y-1.5">{children}</ul>,
  ol: ({ children }) => <ol className="list-decimal pl-6 my-4 text-stone-300 space-y-1.5">{children}</ol>,
  code: ({ children }) => <code className="font-mono text-sm bg-stone-900 text-amber-300 rounded px-1.5 py-0.5">{children}</code>,
  pre: ({ children }) => <pre className="font-mono text-sm bg-stone-950 border border-stone-800 rounded-lg p-4 my-5 overflow-x-auto">{children}</pre>,
  blockquote: ({ children }) => <blockquote className="border-l-2 border-amber-700 pl-4 my-5 text-stone-400 italic">{children}</blockquote>,
}

export function useMDXComponents(): MDXComponents {
  return components
}
```

- [ ] **Step 4: Verificar build**

Run: `npm run build`
Expected: build sem erro com a config MDX (ainda sem posts/rotas de blog — apenas confirma que a config não quebrou).

---

## Task 2: Tipos + parse de frontmatter (TDD)

**Files:**
- Create: `lib/blog/types.ts`
- Create: `lib/blog/parse.ts`
- Test: `lib/blog/parse.test.ts`

- [ ] **Step 1: Criar tipo**

`lib/blog/types.ts`:
```typescript
export type PostMeta = {
  slug: string
  title: string
  date: string // ISO yyyy-mm-dd
  description: string
  tags: string[]
  readingMinutes: number
}
```

- [ ] **Step 2: Escrever teste que falha**

`lib/blog/parse.test.ts`:
```typescript
import { describe, test, expect } from 'vitest'
import { parsePostMeta } from './parse'

const raw = `---
title: Meu Post
date: 2026-05-20
description: Um resumo
tags: [Node.js, AWS]
---

# Olá

Conteúdo com algumas palavras para o cálculo de tempo de leitura.
`

describe('parsePostMeta', () => {
  test('extrai frontmatter + slug', () => {
    const meta = parsePostMeta(raw, 'meu-post')
    expect(meta.slug).toBe('meu-post')
    expect(meta.title).toBe('Meu Post')
    expect(meta.date).toBe('2026-05-20')
    expect(meta.description).toBe('Um resumo')
    expect(meta.tags).toEqual(['Node.js', 'AWS'])
  })
  test('calcula readingMinutes >= 1', () => {
    const meta = parsePostMeta(raw, 'meu-post')
    expect(meta.readingMinutes).toBeGreaterThanOrEqual(1)
  })
})
```

- [ ] **Step 3: Rodar e verificar que falha**

Run: `npm test -- blog/parse`
Expected: FAIL.

- [ ] **Step 4: Implementar**

`lib/blog/parse.ts`:
```typescript
import matter from 'gray-matter'
import readingTime from 'reading-time'
import type { PostMeta } from './types'

export function parsePostMeta(raw: string, slug: string): PostMeta {
  const { data, content } = matter(raw)
  const date = data.date instanceof Date ? data.date.toISOString().slice(0, 10) : String(data.date)
  return {
    slug,
    title: String(data.title ?? ''),
    date,
    description: String(data.description ?? ''),
    tags: Array.isArray(data.tags) ? data.tags.map(String) : [],
    readingMinutes: Math.max(1, Math.ceil(readingTime(content).minutes)),
  }
}
```
> Nota: gray-matter pode parsear `date:` como `Date`; normalizamos para `yyyy-mm-dd`.

- [ ] **Step 5: Rodar e verificar que passa**

Run: `npm test -- blog/parse`
Expected: 2 passed.

---

## Task 3: Filtro/tags (TDD) + posts (FS)

**Files:**
- Create: `lib/blog/filter.ts`
- Create: `lib/blog/posts.ts`
- Test: `lib/blog/filter.test.ts`

- [ ] **Step 1: Teste do filtro (falha)**

`lib/blog/filter.test.ts`:
```typescript
import { describe, test, expect } from 'vitest'
import { filterByTag, allTags } from './filter'
import type { PostMeta } from './types'

const posts: PostMeta[] = [
  { slug: 'a', title: 'A', date: '2026-05-01', description: '', tags: ['Node.js', 'AWS'], readingMinutes: 3 },
  { slug: 'b', title: 'B', date: '2026-04-01', description: '', tags: ['React'], readingMinutes: 2 },
  { slug: 'c', title: 'C', date: '2026-03-01', description: '', tags: ['AWS'], readingMinutes: 5 },
]

describe('filterByTag', () => {
  test('sem tag retorna todos', () => {
    expect(filterByTag(posts, null)).toHaveLength(3)
  })
  test('filtra por tag', () => {
    expect(filterByTag(posts, 'AWS').map((p) => p.slug)).toEqual(['a', 'c'])
  })
})

describe('allTags', () => {
  test('retorna tags únicas ordenadas', () => {
    expect(allTags(posts)).toEqual(['AWS', 'Node.js', 'React'])
  })
})
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm test -- blog/filter`
Expected: FAIL.

- [ ] **Step 3: Implementar filtro**

`lib/blog/filter.ts`:
```typescript
import type { PostMeta } from './types'

export function filterByTag(posts: PostMeta[], tag: string | null): PostMeta[] {
  if (!tag) return posts
  return posts.filter((p) => p.tags.includes(tag))
}

export function allTags(posts: PostMeta[]): string[] {
  return Array.from(new Set(posts.flatMap((p) => p.tags))).sort()
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `npm test -- blog/filter`
Expected: 3 passed.

- [ ] **Step 5: Implementar leitura do FS**

`lib/blog/posts.ts`:
```typescript
import { readdir, readFile } from 'node:fs/promises'
import { join } from 'node:path'
import type { Locale } from '@/lib/i18n/locales'
import type { PostMeta } from './types'
import { parsePostMeta } from './parse'

const BLOG_DIR = join(process.cwd(), 'content', 'blog')

export async function getPostSlugs(locale: Locale): Promise<string[]> {
  try {
    const files = await readdir(join(BLOG_DIR, locale))
    return files.filter((f) => f.endsWith('.mdx')).map((f) => f.replace(/\.mdx$/, ''))
  } catch {
    return []
  }
}

export async function getAllPosts(locale: Locale): Promise<PostMeta[]> {
  const slugs = await getPostSlugs(locale)
  const metas = await Promise.all(
    slugs.map(async (slug) => {
      const raw = await readFile(join(BLOG_DIR, locale, `${slug}.mdx`), 'utf8')
      return parsePostMeta(raw, slug)
    }),
  )
  return metas.sort((a, b) => (a.date < b.date ? 1 : -1)) // mais recente primeiro
}
```

---

## Task 4: BlogPosting JSON-LD (TDD)

**Files:**
- Create: `lib/seo/blog-jsonld.ts`
- Test: `lib/seo/blog-jsonld.test.ts`

- [ ] **Step 1: Teste (falha)**

`lib/seo/blog-jsonld.test.ts`:
```typescript
import { describe, test, expect } from 'vitest'
import { buildBlogPostingJsonLd } from './blog-jsonld'
import type { PostMeta } from '@/lib/blog/types'

const meta: PostMeta = {
  slug: 'meu-post', title: 'Meu Post', date: '2026-05-20',
  description: 'resumo', tags: ['Node.js'], readingMinutes: 4,
}

describe('buildBlogPostingJsonLd', () => {
  test('gera BlogPosting com url canônica do locale', () => {
    const ld = buildBlogPostingJsonLd(meta, 'pt')
    expect(ld['@type']).toBe('BlogPosting')
    expect(ld.headline).toBe('Meu Post')
    expect(ld.datePublished).toBe('2026-05-20')
    expect(ld.url).toBe('https://caioweliton.dev/pt/blog/meu-post')
    expect((ld.author as { name: string }).name).toBe('Caio Weliton')
  })
})
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm test -- blog-jsonld`
Expected: FAIL.

- [ ] **Step 3: Implementar**

`lib/seo/blog-jsonld.ts`:
```typescript
import type { Locale } from '@/lib/i18n/locales'
import type { PostMeta } from '@/lib/blog/types'
import { site } from '@/data/site'
import { SITE_URL } from './site-url'

export function buildBlogPostingJsonLd(meta: PostMeta, locale: Locale): Record<string, unknown> {
  return {
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: meta.title,
    description: meta.description,
    datePublished: meta.date,
    dateModified: meta.date,
    keywords: meta.tags.join(', '),
    url: `${SITE_URL}/${locale}/blog/${meta.slug}`,
    author: { '@type': 'Person', name: site.name, url: SITE_URL },
  }
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `npm test -- blog-jsonld`
Expected: 1 passed.

---

## Task 5: Posts seed (PT + EN)

**Files:**
- Create: `content/blog/pt/estruturando-microservicos-node.mdx`
- Create: `content/blog/en/structuring-node-microservices.mdx`

- [ ] **Step 1: Post PT**

`content/blog/pt/estruturando-microservicos-node.mdx`:
```mdx
---
title: Como estruturo microserviços Node.js em produção
date: 2026-05-20
description: Padrões que uso para manter serviços desacoplados, observáveis e fáceis de evoluir.
tags: [Node.js, Arquitetura, AWS]
---

# Como estruturo microserviços Node.js em produção

Depois de alguns anos construindo back-ends que precisam escalar, alguns padrões se repetem. Aqui vão os que mais me ajudam no dia a dia.

## Fronteiras claras

Cada serviço dono do seu dado. Comunicação por contrato explícito — nada de compartilhar tabela entre serviços.

## Observabilidade desde o dia zero

Log estruturado, métricas e tracing distribuído. Sem isso, debugar em produção vira adivinhação.

> Código é só metade do trabalho — operar bem é a outra metade.
```

- [ ] **Step 2: Post EN**

`content/blog/en/structuring-node-microservices.mdx`:
```mdx
---
title: How I structure Node.js microservices in production
date: 2026-05-20
description: Patterns I use to keep services decoupled, observable and easy to evolve.
tags: [Node.js, Architecture, AWS]
---

# How I structure Node.js microservices in production

After a few years building back-ends that must scale, some patterns keep showing up. Here are the ones that help me the most.

## Clear boundaries

Each service owns its data. Communication via explicit contracts — never share a table between services.

## Observability from day zero

Structured logs, metrics and distributed tracing. Without them, debugging in production becomes guesswork.

> Code is only half the job — operating it well is the other half.
```

---

## Task 6: Página da lista `/blog` + filtro (client) + teste

**Files:**
- Create: `components/blog/post-list.tsx`
- Create: `app/[locale]/blog/page.tsx`
- Test: `components/blog/post-list.test.tsx`

- [ ] **Step 1: Teste do PostList (falha)**

`components/blog/post-list.test.tsx`:
```tsx
import { describe, test, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { PostList } from './post-list'
import type { PostMeta } from '@/lib/blog/types'

vi.mock('next/link', () => ({ default: ({ children, href }: { children: React.ReactNode; href: string }) => <a href={href}>{children}</a> }))

const posts: PostMeta[] = [
  { slug: 'a', title: 'Post AWS', date: '2026-05-01', description: '', tags: ['AWS'], readingMinutes: 3 },
  { slug: 'b', title: 'Post React', date: '2026-04-01', description: '', tags: ['React'], readingMinutes: 2 },
]

describe('PostList', () => {
  test('lista todos os posts', () => {
    render(<PostList locale="pt" posts={posts} />)
    expect(screen.getByText('Post AWS')).toBeInTheDocument()
    expect(screen.getByText('Post React')).toBeInTheDocument()
  })
  test('filtra por tag', async () => {
    const user = userEvent.setup()
    render(<PostList locale="pt" posts={posts} />)
    await user.click(screen.getByRole('button', { name: 'AWS' }))
    expect(screen.getByText('Post AWS')).toBeInTheDocument()
    expect(screen.queryByText('Post React')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm test -- post-list`
Expected: FAIL.

- [ ] **Step 3: Implementar PostList (client)**

`components/blog/post-list.tsx`:
```tsx
'use client'

import { useState } from 'react'
import Link from 'next/link'
import type { Locale } from '@/lib/i18n/locales'
import type { PostMeta } from '@/lib/blog/types'
import { filterByTag, allTags } from '@/lib/blog/filter'

export function PostList({ locale, posts }: { locale: Locale; posts: PostMeta[] }) {
  const [tag, setTag] = useState<string | null>(null)
  const tags = allTags(posts)
  const shown = filterByTag(posts, tag)

  return (
    <div>
      <div className="flex gap-2 flex-wrap mb-8">
        <button onClick={() => setTag(null)}
          className={`font-mono text-xs rounded px-3 py-1 transition-colors ${tag === null ? 'bg-amber-600 text-white' : 'border border-stone-800 text-stone-500 hover:text-stone-300'}`}>
          {locale === 'pt' ? 'todos' : 'all'}
        </button>
        {tags.map((t) => (
          <button key={t} onClick={() => setTag(t)}
            className={`font-mono text-xs rounded px-3 py-1 transition-colors ${tag === t ? 'bg-amber-600 text-white' : 'border border-stone-800 text-stone-500 hover:text-stone-300'}`}>
            {t}
          </button>
        ))}
      </div>
      <div className="flex flex-col">
        {shown.map((p) => (
          <Link key={p.slug} href={`/${locale}/blog/${p.slug}`}
            className="group flex items-center justify-between py-4 border-b border-stone-900 hover:border-stone-700 transition-colors">
            <div>
              <div className="text-stone-100 font-medium group-hover:text-amber-400 transition-colors">{p.title}</div>
              <div className="flex gap-1.5 items-center mt-1 font-mono text-[10px] text-stone-600">
                {p.tags[0] && <span className="text-amber-700">{p.tags[0]}</span>}
                <span>·</span>
                <span>{p.readingMinutes} min</span>
              </div>
            </div>
            <span className="font-mono text-xs text-stone-600 shrink-0 ml-4">{p.date}</span>
          </Link>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `npm test -- post-list`
Expected: 2 passed.

- [ ] **Step 5: Página da lista (server)**

`app/[locale]/blog/page.tsx`:
```tsx
import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { isLocale, type Locale } from '@/lib/i18n/locales'
import { getAllPosts } from '@/lib/blog/posts'
import { buildMetadata } from '@/lib/seo/metadata'
import { PostList } from '@/components/blog/post-list'

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params
  const l = isLocale(locale) ? (locale as Locale) : 'pt'
  return buildMetadata({
    locale: l, path: '/blog',
    title: 'Blog — Caio Weliton',
    description: l === 'pt'
      ? 'Artigos sobre Node.js, AWS, arquitetura e liderança técnica.'
      : 'Articles about Node.js, AWS, architecture and technical leadership.',
  })
}

export default async function BlogPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params
  if (!isLocale(locale)) notFound()
  const l = locale as Locale
  const posts = await getAllPosts(l)

  return (
    <section className="px-6 py-20 max-w-3xl">
      <p className="font-mono text-xs tracking-[0.2em] text-amber-600 mb-2">№ 06 — BLOG</p>
      <h1 className="text-2xl md:text-3xl font-bold tracking-tight text-stone-50 mb-8">
        {l === 'pt' ? 'Escritos' : 'Writing'}
      </h1>
      <PostList locale={l} posts={posts} />
    </section>
  )
}
```

---

## Task 7: Post individual `/blog/[slug]`

**Files:**
- Create: `app/[locale]/blog/[slug]/page.tsx`

- [ ] **Step 1: Implementar a rota do post**

```tsx
import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { isLocale, locales, type Locale } from '@/lib/i18n/locales'
import { getAllPosts, getPostSlugs } from '@/lib/blog/posts'
import { buildMetadata } from '@/lib/seo/metadata'
import { buildBlogPostingJsonLd } from '@/lib/seo/blog-jsonld'
import { JsonLd } from '@/components/json-ld'

export const dynamicParams = false

export async function generateStaticParams() {
  const params: { locale: string; slug: string }[] = []
  for (const locale of locales) {
    for (const slug of await getPostSlugs(locale)) {
      params.push({ locale, slug })
    }
  }
  return params
}

async function getMeta(locale: Locale, slug: string) {
  const posts = await getAllPosts(locale)
  return posts.find((p) => p.slug === slug)
}

export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> }): Promise<Metadata> {
  const { locale, slug } = await params
  const l = isLocale(locale) ? (locale as Locale) : 'pt'
  const meta = await getMeta(l, slug)
  if (!meta) return {}
  return buildMetadata({ locale: l, path: `/blog/${slug}`, title: `${meta.title} — Caio Weliton`, description: meta.description })
}

export default async function PostPage({ params }: { params: Promise<{ locale: string; slug: string }> }) {
  const { locale, slug } = await params
  if (!isLocale(locale)) notFound()
  const l = locale as Locale

  const meta = await getMeta(l, slug)
  if (!meta) notFound()

  const { default: Post } = await import(`@/content/blog/${l}/${slug}.mdx`)

  return (
    <article className="px-6 py-20 max-w-2xl">
      <JsonLd data={buildBlogPostingJsonLd(meta, l)} />
      <p className="font-mono text-xs text-stone-600 mb-2">{meta.date} · {meta.readingMinutes} min</p>
      <div className="flex gap-1.5 mb-8 font-mono text-[10px]">
        {meta.tags.map((t) => <span key={t} className="text-amber-300 bg-amber-950/30 rounded px-2 py-0.5">{t}</span>)}
      </div>
      <Post />
      <div className="mt-12 pt-6 border-t border-stone-900">
        <a href={`/${l}/blog`} className="font-mono text-xs text-amber-500 hover:text-amber-400">← {l === 'pt' ? 'voltar' : 'back'}</a>
      </div>
    </article>
  )
}
```
> O import dinâmico `@/content/blog/${l}/${slug}.mdx` é resolvido pelo @next/mdx; `dynamicParams=false` + `generateStaticParams` pré-renderam só os posts existentes.

- [ ] **Step 2: Build + verificação**

Run: `npm run build`
Expected: rotas `/[locale]/blog` e `/[locale]/blog/[slug]` no output (pt+en × 1 slug cada lado). Sem erro.
Run (background) `npm run dev`: abrir `/pt/blog` (lista + filtro de tag), clicar no post → render MDX estilizado, ver JSON-LD `BlogPosting` no source. Repetir `/en/blog`. Encerrar dev.

---

## Task 8: BlogPreview na landing + sitemap

**Files:**
- Create: `components/sections/blog-preview.tsx`
- Modify: `app/[locale]/page.tsx`
- Modify: `app/sitemap.ts`
- Modify: `app/sitemap.test.ts`

- [ ] **Step 1: BlogPreview (server)**

`components/sections/blog-preview.tsx`:
```tsx
import Link from 'next/link'
import type { Locale } from '@/lib/i18n/locales'
import { getAllPosts } from '@/lib/blog/posts'
import { SectionHeading } from '@/components/sections/section-heading'

export async function BlogPreview({ locale }: { locale: Locale }) {
  const posts = (await getAllPosts(locale)).slice(0, 3)
  if (posts.length === 0) return null

  return (
    <section id="blog" className="px-6 py-20">
      <SectionHeading index="06" label="Blog" title={locale === 'pt' ? 'Escritos recentes' : 'Recent writing'} />
      <div className="flex flex-col">
        {posts.map((p) => (
          <Link key={p.slug} href={`/${locale}/blog/${p.slug}`}
            className="group flex items-center justify-between py-4 border-b border-stone-900 hover:border-stone-700 transition-colors">
            <div>
              <div className="text-stone-100 font-medium group-hover:text-amber-400 transition-colors">{p.title}</div>
              <div className="font-mono text-[10px] text-stone-600 mt-1">{p.tags[0]} · {p.readingMinutes} min</div>
            </div>
            <span className="font-mono text-xs text-stone-600 shrink-0 ml-4">{p.date}</span>
          </Link>
        ))}
      </div>
      <Link href={`/${locale}/blog`} className="inline-block mt-6 font-mono text-xs text-amber-500 hover:text-amber-400">
        {locale === 'pt' ? 'ver todos os posts' : 'see all posts'} →
      </Link>
    </section>
  )
}
```

- [ ] **Step 2: Injetar na landing**

Em `app/[locale]/page.tsx`, importar `BlogPreview` e inserir entre `<Skills />` e `<Contact />`:
```tsx
import { BlogPreview } from '@/components/sections/blog-preview'
// ...
      <Skills locale={l} />
      <BlogPreview locale={l} />
      <Contact locale={l} />
```
(BlogPreview é async Server Component — ok dentro do componente async da página.)

- [ ] **Step 3: Atualizar teste do sitemap (falha)**

Adicionar caso em `app/sitemap.test.ts` e ajustar os existentes para `await sitemap()`:
```typescript
  test('inclui posts do blog', async () => {
    const entries = await sitemap()
    const urls = entries.map((e) => e.url)
    expect(urls.some((u) => u.includes('/blog/'))).toBe(true)
  })
```
> O sitemap passa a ser `async`. Ajustar o teste existente (`inclui a home de cada locale`) para `const urls = (await sitemap()).map(...)`.

- [ ] **Step 4: Rodar e verificar que falha**

Run: `npm test -- sitemap`
Expected: FAIL (sitemap ainda síncrono / sem posts).

- [ ] **Step 5: Tornar o sitemap async + incluir posts**

`app/sitemap.ts`:
```typescript
import type { MetadataRoute } from 'next'
import { locales } from '@/lib/i18n/locales'
import { SITE_URL } from '@/lib/seo/site-url'
import { getAllPosts } from '@/lib/blog/posts'

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const entries: MetadataRoute.Sitemap = []
  for (const locale of locales) {
    entries.push({ url: `${SITE_URL}/${locale}`, lastModified: new Date(), changeFrequency: 'monthly', priority: 1 })
    entries.push({ url: `${SITE_URL}/${locale}/blog`, lastModified: new Date(), changeFrequency: 'weekly', priority: 0.8 })
    for (const post of await getAllPosts(locale)) {
      entries.push({ url: `${SITE_URL}/${locale}/blog/${post.slug}`, lastModified: new Date(post.date), changeFrequency: 'monthly', priority: 0.6 })
    }
  }
  return entries
}
```

- [ ] **Step 6: Rodar e verificar que passa**

Run: `npm test -- sitemap`
Expected: testes do sitemap passam (home de cada locale + posts).

---

## Task 9: Verificação final + commit da fase

- [ ] **Step 1: Gate completo**

Run: `npm run lint && npm test && npm run build`
Expected: lint limpo; testes (23 da Fase 2 + parse 2 + filter 3 + blog-jsonld 1 + post-list 2 + sitemap +1 = **~32**); build com rotas de blog, lista e posts estáticos.

- [ ] **Step 2: Validação manual**

`npm run dev`: `/pt/blog` e `/en/blog` (lista + filtro), abrir um post (MDX estilizado, JSON-LD BlogPosting), landing com BlogPreview entre Skills e Contact, `/sitemap.xml` com URLs de blog. Encerrar dev.

- [ ] **Step 3: Propor commit ao dono (NÃO executar sem aprovação)**

```bash
git add -A
git commit -m "feat: blog MDX — lista /blog com filtro, post /blog/[slug], BlogPosting JSON-LD, preview na landing"
```
**Aguardar aprovação. Sem push.**

---

## Self-Review (cobertura vs spec)

- **MDX em `content/blog/*` com frontmatter** → Tasks 1, 5 ✓ (estrutura `content/blog/<locale>/<slug>.mdx`)
- **Lista + filtro por tag** → Tasks 3, 6 ✓
- **`/blog/[slug]` render + generateMetadata + generateStaticParams** → Task 7 ✓
- **BlogPosting JSON-LD** → Tasks 4, 7 ✓
- **reading-time** → Task 2 ✓
- **Preview na landing (3 recentes)** → Task 8 ✓
- **Posts indexados no sitemap** → Task 8 ✓

Fora do escopo (próximas fases): Now/Uses + CV PDF (Fase 4); animações, light-mode, syntax highlighting de code blocks, RSS feed, paginação do blog (Fase 5 ou backlog).

## Notas de consistência de tipos

- `PostMeta` em `lib/blog/types.ts`: `{ slug, title, date(yyyy-mm-dd), description, tags[], readingMinutes }`.
- `parsePostMeta(raw, slug) → PostMeta` (puro); `getAllPosts(locale) → Promise<PostMeta[]>` (FS, ordena desc por data); `getPostSlugs(locale) → Promise<string[]>`.
- `filterByTag(posts, tag|null)`, `allTags(posts)` puros.
- `buildBlogPostingJsonLd(meta, locale) → objeto`; render via `<JsonLd data={...} />`.
- `app/sitemap.ts` passa a ser **async** (`getAllPosts` é async) — teste ajustado para `await sitemap()`.
- Estrutura de arquivos dos posts: `content/blog/<locale>/<slug>.mdx`; import dinâmico `@/content/blog/${locale}/${slug}.mdx`.
